import { View, Text } from "react-native";

const Arm = () => {
    return(
        <View
            style={{
                flex: 1,
                backgroundColor: "pink"
            }}>
            <Text>Arm</Text>
        </View>
    )
}

export default Arm;